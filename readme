Линейный однонаправленный список — это структура данных, состоящая из элементов одного типа*, связанных между
собой последовательно посредством указателей.

Для связного списка:

    Каждый элемент списка имеет указатель на следующий элемент.
    Последний элемент списка указывает на NULL.
    Элемент, на который нет указателя, является первым (головным) элементом списка.


*Точно одного? В нашем случае вполне себе произвольного, любое не итерируемое значение будет храниться в 1 узле,
итерируемое будет растащено по отдельным узлам
